# Fonzygrok Docker environment configuration.
# Copy to .env and customize as needed.
FONZYGROK_VERSION=dev
DOMAIN=tunnel.localhost
SSH_PORT=2222
HTTP_PORT=8080
HTTPS_PORT=443
ADMIN_PORT=9090
TLS_ENABLED=false
