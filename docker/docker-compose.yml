services:
  fonzygrok-server:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      args:
        VERSION: "${FONZYGROK_VERSION:-dev}"
    container_name: fonzygrok-server
    restart: unless-stopped
    ports:
      - "${SSH_PORT:-2222}:2222"
      - "${HTTP_PORT:-8080}:8080"
      - "${HTTPS_PORT:-443}:443"
      # Port 9090 is optional for external access — dashboard is served on :443
      # when TLS is enabled. Uncomment for direct admin API access.
      # - "${ADMIN_PORT:-9090}:9090"
    volumes:
      - fonzygrok-data:/data
      - fonzygrok-certs:/data/certs
    environment:
      - DOMAIN=${DOMAIN:-tunnel.localhost}
      - APEX_DOMAIN=${APEX_DOMAIN:-}
      - TLS_ENABLED=${TLS_ENABLED:-false}
    entrypoint: ["/usr/local/bin/entrypoint.sh"]
    command: []
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:9090/api/v1/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s

volumes:
  fonzygrok-data:
    driver: local
  fonzygrok-certs:
    driver: local
